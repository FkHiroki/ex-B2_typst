= 3. 実験方法

== 3.1 レーザー光による回折強度パターン

  今回は、レーザ光を用いて、スリットの回折強度パターンを観察することを実際には行わず、与えられた実験データをImageJを用いて解析することで、スリットの幅や個数による回折強度パターンの変化を観察した。まず、与えられた実験データには、3種類の幅の単スリットを用いた時のスリット画像とスクリーン画像の計6枚、幅やスリット間隔を変えた3種類のダブルスリットを用いた時のスリット画像とスクリーンの計6枚である。まず、単スリットの場合は、ImageJを用いてスリットの幅を測定し、また、精密な目盛りを顕微鏡で撮影した画像を用いて、pixel単位をm単位に変換した。次に、ImageJによってスクリーン画像の光強度のグラフを得た。光強度のグラフから、強度パターンの周期を測定し


== 3.2 回折強度パターンのシミュレーション

#figure(
  image("/figs/slit.png", width: 50%),
  caption: [単スリット（幅: 0.1）]
  ) <fig:>

  #figure(  
  image("/figs/slit_wave.png", width: 50%),
  caption: [単スリットの関数]
  ) <fig:>

  #figure(
  image("/figs/slit_screen.png", width: 50%),
  caption: [単スリットのスクリーン]
  ) <fig:>

  #figure(
    image("/figs/slit_0.2.png", width: 50%),
    caption: [単スリット（幅: 0.2）]
  ) <fig:>

  #figure(
    image("/figs/slit_0.2_wave.png", width: 50%),
    caption: [単スリットの関数（幅: 0.2）]
  ) <fig:>

  #figure(
    image("/figs/slit_0.2_screen.png", width: 50%),
    caption: [単スリットのスクリーン（幅: 0.2）]
  ) <fig:>

